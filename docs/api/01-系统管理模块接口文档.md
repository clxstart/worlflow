# WorkFlow 系统接口文档

## 文档说明

- **基础路径**: `http://localhost:8080`
- **认证方式**: JWT Token（Header: Authorization: Bearer {token}）
- **响应格式**: JSON

## 通用响应格式

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {}
}
```

## 通用状态码

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权/Token失效 |
| 403 | 权限不足 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 模块索引

1. [认证模块](#1-认证模块)
2. [用户管理](#2-用户管理)
3. [角色管理](#3-角色管理)
4. [菜单管理](#4-菜单管理)
5. [部门管理](#5-部门管理)
6. [岗位管理](#6-岗位管理)
7. [字典管理](#7-字典管理)
8. [参数配置](#8-参数配置)
9. [通知公告](#9-通知公告)
10. [操作日志](#10-操作日志)
11. [登录日志](#11-登录日志)

---

## 1. 认证模块

### 1.1 用户登录

**接口地址**: `POST /login`

**是否认证**: 否

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |
| captcha | string | 否 | 验证码 |
| uuid | string | 否 | 验证码UUID |

**请求示例**:
```json
{
  "username": "admin",
  "password": "123456"
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

### 1.2 用户登出

**接口地址**: `POST /logout`

**是否认证**: 是

**请求参数**: 无

**响应示例**:
```json
{
  "code": 200,
  "msg": "登出成功"
}
```

### 1.3 获取验证码

**接口地址**: `GET /captcha`

**是否认证**: 否

**请求参数**: 无

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "uuid": "xxx-xxx-xxx",
    "img": "data:image/png;base64,..."
  }
}
```

### 1.4 获取当前用户信息

**接口地址**: `GET /getInfo`

**是否认证**: 是

**请求参数**: 无

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "user": {
      "userId": 1,
      "userName": "admin",
      "nickName": "管理员",
      "email": "admin@clx.com",
      "phonenumber": "15888888888",
      "sex": "0",
      "avatar": null,
      "status": "0",
      "deptId": 103
    },
    "roles": ["admin"],
    "permissions": ["*:*:*"]
  }
}
```

### 1.5 获取路由信息

**接口地址**: `GET /getRouters`

**是否认证**: 是

**请求参数**: 无

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "name": "System",
      "path": "/system",
      "component": "Layout",
      "meta": {
        "title": "系统管理",
        "icon": "system"
      },
      "children": [
        {
          "name": "User",
          "path": "user",
          "component": "system/user/index",
          "meta": {
            "title": "用户管理",
            "icon": "user"
          }
        }
      ]
    }
  ]
}
```

---

## 2. 用户管理

### 2.1 获取用户列表

**接口地址**: `GET /system/user/list`

**是否认证**: 是

**权限标识**: `system:user:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | int | 否 | 页码，默认1 |
| pageSize | int | 否 | 每页数量，默认10 |
| userName | string | 否 | 用户名（模糊查询） |
| nickName | string | 否 | 昵称（模糊查询） |
| phonenumber | string | 否 | 手机号 |
| status | string | 否 | 状态（0正常 1停用） |
| deptId | long | 否 | 部门ID |
| beginTime | string | 否 | 开始时间 |
| endTime | string | 否 | 结束时间 |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 100,
    "rows": [
      {
        "userId": 1,
        "userName": "admin",
        "nickName": "管理员",
        "email": "admin@clx.com",
        "phonenumber": "15888888888",
        "sex": "0",
        "status": "0",
        "createTime": "2024-01-01 00:00:00",
        "dept": {
          "deptId": 103,
          "deptName": "研发部门"
        },
        "roles": [
          {
            "roleId": 1,
            "roleName": "超级管理员"
          }
        ]
      }
    ]
  }
}
```

### 2.2 获取用户详情

**接口地址**: `GET /system/user/{userId}`

**是否认证**: 是

**权限标识**: `system:user:query`

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userId | long | 是 | 用户ID |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "userId": 1,
    "userName": "admin",
    "nickName": "管理员",
    "email": "admin@clx.com",
    "phonenumber": "15888888888",
    "sex": "0",
    "avatar": null,
    "status": "0",
    "deptId": 103,
    "postIds": [1, 2],
    "roleIds": [1],
    "remark": "管理员"
  }
}
```

### 2.3 新增用户

**接口地址**: `POST /system/user`

**是否认证**: 是

**权限标识**: `system:user:add`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userName | string | 是 | 用户名 |
| nickName | string | 是 | 昵称 |
| password | string | 是 | 密码 |
| email | string | 否 | 邮箱 |
| phonenumber | string | 否 | 手机号 |
| sex | string | 否 | 性别（0男 1女 2未知） |
| status | string | 否 | 状态（0正常 1停用） |
| deptId | long | 否 | 部门ID |
| postIds | array | 否 | 岗位ID数组 |
| roleIds | array | 否 | 角色ID数组 |
| remark | string | 否 | 备注 |

**请求示例**:
```json
{
  "userName": "zhangsan",
  "nickName": "张三",
  "password": "123456",
  "email": "zhangsan@clx.com",
  "phonenumber": "15899999999",
  "sex": "0",
  "status": "0",
  "deptId": 103,
  "postIds": [2],
  "roleIds": [2]
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "新增成功"
}
```

### 2.4 修改用户

**接口地址**: `PUT /system/user`

**是否认证**: 是

**权限标识**: `system:user:edit`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userId | long | 是 | 用户ID |
| nickName | string | 是 | 昵称 |
| email | string | 否 | 邮箱 |
| phonenumber | string | 否 | 手机号 |
| sex | string | 否 | 性别 |
| status | string | 否 | 状态 |
| deptId | long | 否 | 部门ID |
| postIds | array | 否 | 岗位ID数组 |
| roleIds | array | 否 | 角色ID数组 |
| remark | string | 否 | 备注 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 2.5 删除用户

**接口地址**: `DELETE /system/user/{userIds}`

**是否认证**: 是

**权限标识**: `system:user:remove`

**路径参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userIds | string | 是 | 用户ID，多个用逗号分隔 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

### 2.6 重置密码

**接口地址**: `PUT /system/user/resetPwd`

**是否认证**: 是

**权限标识**: `system:user:resetPwd`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userId | long | 是 | 用户ID |
| password | string | 是 | 新密码 |

**请求示例**:
```json
{
  "userId": 2,
  "password": "123456"
}
```

**响应示例**:
```json
{
  "code": 200,
  "msg": "密码重置成功"
}
```

### 2.7 修改用户状态

**接口地址**: `PUT /system/user/changeStatus`

**是否认证**: 是

**权限标识**: `system:user:edit`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userId | long | 是 | 用户ID |
| status | string | 是 | 状态（0正常 1停用） |

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 2.8 导出用户

**接口地址**: `POST /system/user/export`

**是否认证**: 是

**权限标识**: `system:user:export`

**请求参数**: 同列表查询参数

**响应**: Excel文件下载

### 2.9 导入用户

**接口地址**: `POST /system/user/importData`

**是否认证**: 是

**权限标识**: `system:user:import`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | file | 是 | Excel文件 |
| updateSupport | boolean | 否 | 是否更新已存在数据 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "导入成功"
}
```

### 2.10 下载导入模板

**接口地址**: `POST /system/user/importTemplate`

**是否认证**: 是

**响应**: Excel模板文件下载

### 2.11 校验用户名唯一性

**接口地址**: `GET /system/user/checkUserNameUnique`

**是否认证**: 是

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userName | string | 是 | 用户名 |

**响应示例**:
```json
{
  "code": 200,
  "data": true
}
```

### 2.12 校验手机号唯一性

**接口地址**: `GET /system/user/checkPhoneUnique`

**是否认证**: 是

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| phonenumber | string | 是 | 手机号 |
| userId | long | 否 | 用户ID（修改时传） |

**响应示例**:
```json
{
  "code": 200,
  "data": true
}
```

### 2.13 校验邮箱唯一性

**接口地址**: `GET /system/user/checkEmailUnique`

**是否认证**: 是

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| email | string | 是 | 邮箱 |
| userId | long | 否 | 用户ID（修改时传） |

**响应示例**:
```json
{
  "code": 200,
  "data": true
}
```

---

## 3. 角色管理

### 3.1 获取角色列表

**接口地址**: `GET /system/role/list`

**是否认证**: 是

**权限标识**: `system:role:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |
| roleName | string | 否 | 角色名称 |
| roleKey | string | 否 | 权限字符 |
| status | string | 否 | 状态 |
| beginTime | string | 否 | 开始时间 |
| endTime | string | 否 | 结束时间 |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 10,
    "rows": [
      {
        "roleId": 1,
        "roleName": "超级管理员",
        "roleKey": "admin",
        "roleSort": 1,
        "status": "0",
        "createTime": "2024-01-01 00:00:00"
      }
    ]
  }
}
```

### 3.2 获取角色详情

**接口地址**: `GET /system/role/{roleId}`

**是否认证**: 是

**权限标识**: `system:role:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "roleId": 1,
    "roleName": "超级管理员",
    "roleKey": "admin",
    "roleSort": 1,
    "status": "0",
    "menuIds": [1, 2, 3],
    "remark": "超级管理员"
  }
}
```

### 3.3 新增角色

**接口地址**: `POST /system/role`

**是否认证**: 是

**权限标识**: `system:role:add`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| roleName | string | 是 | 角色名称 |
| roleKey | string | 是 | 权限字符 |
| roleSort | int | 是 | 显示顺序 |
| status | string | 否 | 状态 |
| menuIds | array | 否 | 菜单ID数组 |
| remark | string | 否 | 备注 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "新增成功"
}
```

### 3.4 修改角色

**接口地址**: `PUT /system/role`

**是否认证**: 是

**权限标识**: `system:role:edit`

**请求参数**: 同新增，需包含roleId

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 3.5 删除角色

**接口地址**: `DELETE /system/role/{roleIds}`

**是否认证**: 是

**权限标识**: `system:role:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

### 3.6 修改角色状态

**接口地址**: `PUT /system/role/changeStatus`

**是否认证**: 是

**权限标识**: `system:role:edit`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| roleId | long | 是 | 角色ID |
| status | string | 是 | 状态 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 3.7 获取角色菜单树

**接口地址**: `GET /system/role/menuTreeselect/{roleId}`

**是否认证**: 是

**权限标识**: `system:role:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "checkedKeys": [1, 2, 3],
    "menus": [
      {
        "id": 1,
        "label": "系统管理",
        "children": []
      }
    ]
  }
}
```

### 3.8 数据权限修改

**接口地址**: `PUT /system/role/dataScope`

**是否认证**: 是

**权限标识**: `system:role:edit`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| roleId | long | 是 | 角色ID |
| dataScope | string | 是 | 数据范围（1全部 2自定义 3本部门 4本部门及以下 5仅本人） |
| deptIds | array | 否 | 部门ID数组（自定义时必填） |

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 3.9 校验角色名称唯一性

**接口地址**: `GET /system/role/checkRoleNameUnique`

**是否认证**: 是

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| roleName | string | 是 | 角色名称 |
| roleId | long | 否 | 角色ID |

**响应示例**:
```json
{
  "code": 200,
  "data": true
}
```

### 3.10 校验权限字符唯一性

**接口地址**: `GET /system/role/checkRoleKeyUnique`

**是否认证**: 是

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| roleKey | string | 是 | 权限字符 |
| roleId | long | 否 | 角色ID |

**响应示例**:
```json
{
  "code": 200,
  "data": true
}
```

---

## 4. 菜单管理

### 4.1 获取菜单列表

**接口地址**: `GET /system/menu/list`

**是否认证**: 是

**权限标识**: `system:menu:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| menuName | string | 否 | 菜单名称 |
| status | string | 否 | 状态 |

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "menuId": 1,
      "menuName": "系统管理",
      "parentId": 0,
      "orderNum": 1,
      "path": "system",
      "component": null,
      "menuType": "M",
      "visible": "0",
      "status": "0",
      "perms": null,
      "icon": "system",
      "createTime": "2024-01-01 00:00:00",
      "children": []
    }
  ]
}
```

### 4.2 获取菜单详情

**接口地址**: `GET /system/menu/{menuId}`

**是否认证**: 是

**权限标识**: `system:menu:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "menuId": 1,
    "menuName": "系统管理",
    "parentId": 0,
    "orderNum": 1,
    "path": "system",
    "component": null,
    "query": null,
    "isFrame": 1,
    "isCache": 0,
    "menuType": "M",
    "visible": "0",
    "status": "0",
    "perms": null,
    "icon": "system",
    "remark": "系统管理目录"
  }
}
```

### 4.3 新增菜单

**接口地址**: `POST /system/menu`

**是否认证**: 是

**权限标识**: `system:menu:add`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| menuName | string | 是 | 菜单名称 |
| parentId | long | 否 | 父菜单ID |
| orderNum | int | 否 | 显示顺序 |
| path | string | 否 | 路由地址 |
| component | string | 否 | 组件路径 |
| query | string | 否 | 路由参数 |
| isFrame | int | 否 | 是否外链（0否 1是） |
| isCache | int | 否 | 是否缓存（0否 1是） |
| menuType | string | 否 | 类型（M目录 C菜单 F按钮） |
| visible | string | 否 | 显示状态（0显示 1隐藏） |
| status | string | 否 | 菜单状态（0正常 1停用） |
| perms | string | 否 | 权限标识 |
| icon | string | 否 | 菜单图标 |
| remark | string | 否 | 备注 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "新增成功"
}
```

### 4.4 修改菜单

**接口地址**: `PUT /system/menu`

**是否认证**: 是

**权限标识**: `system:menu:edit`

**请求参数**: 同新增，需包含menuId

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 4.5 删除菜单

**接口地址**: `DELETE /system/menu/{menuId}`

**是否认证**: 是

**权限标识**: `system:menu:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

### 4.6 获取菜单下拉树

**接口地址**: `GET /system/menu/treeselect`

**是否认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "label": "系统管理",
      "children": [
        {
          "id": 2,
          "label": "用户管理",
          "children": []
        }
      ]
    }
  ]
}
```

### 4.7 获取角色菜单树

**接口地址**: `GET /system/menu/roleMenuTreeselect/{roleId}`

**是否认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "checkedKeys": [1, 2, 3],
    "menus": []
  }
}
```

---

## 5. 部门管理

### 5.1 获取部门列表

**接口地址**: `GET /system/dept/list`

**是否认证**: 是

**权限标识**: `system:dept:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| deptName | string | 否 | 部门名称 |
| status | string | 否 | 状态 |

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "deptId": 100,
      "parentId": 0,
      "deptName": "总公司",
      "orderNum": 0,
      "leader": null,
      "phone": null,
      "email": null,
      "status": "0",
      "createTime": "2024-01-01 00:00:00",
      "children": [
        {
          "deptId": 101,
          "parentId": 100,
          "deptName": "深圳分公司",
          "children": []
        }
      ]
    }
  ]
}
```

### 5.2 获取部门详情

**接口地址**: `GET /system/dept/{deptId}`

**是否认证**: 是

**权限标识**: `system:dept:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "deptId": 100,
    "parentId": 0,
    "ancestors": "0",
    "deptName": "总公司",
    "orderNum": 0,
    "leader": null,
    "phone": null,
    "email": null,
    "status": "0",
    "remark": null
  }
}
```

### 5.3 新增部门

**接口地址**: `POST /system/dept`

**是否认证**: 是

**权限标识**: `system:dept:add`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| parentId | long | 是 | 父部门ID |
| deptName | string | 是 | 部门名称 |
| orderNum | int | 否 | 显示顺序 |
| leader | string | 否 | 负责人 |
| phone | string | 否 | 联系电话 |
| email | string | 否 | 邮箱 |
| status | string | 否 | 状态 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "新增成功"
}
```

### 5.4 修改部门

**接口地址**: `PUT /system/dept`

**是否认证**: 是

**权限标识**: `system:dept:edit`

**请求参数**: 同新增，需包含deptId

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 5.5 删除部门

**接口地址**: `DELETE /system/dept/{deptId}`

**是否认证**: 是

**权限标识**: `system:dept:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

### 5.6 获取部门下拉树

**接口地址**: `GET /system/dept/treeselect`

**是否认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 100,
      "label": "总公司",
      "children": []
    }
  ]
}
```

### 5.7 获取角色部门树

**接口地址**: `GET /system/dept/roleDeptTreeselect/{roleId}`

**是否认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "checkedKeys": [103, 104],
    "depts": []
  }
}
```

### 5.8 校验部门名称唯一性

**接口地址**: `GET /system/dept/checkDeptNameUnique`

**是否认证**: 是

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| deptName | string | 是 | 部门名称 |
| parentId | long | 是 | 父部门ID |
| deptId | long | 否 | 部门ID |

**响应示例**:
```json
{
  "code": 200,
  "data": true
}
```

---

## 6. 岗位管理

### 6.1 获取岗位列表

**接口地址**: `GET /system/post/list`

**是否认证**: 是

**权限标识**: `system:post:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |
| postCode | string | 否 | 岗位编码 |
| postName | string | 否 | 岗位名称 |
| status | string | 否 | 状态 |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 4,
    "rows": [
      {
        "postId": 1,
        "postCode": "ceo",
        "postName": "董事长",
        "postSort": 1,
        "status": "0",
        "createTime": "2024-01-01 00:00:00"
      }
    ]
  }
}
```

### 6.2 获取岗位详情

**接口地址**: `GET /system/post/{postId}`

**是否认证**: 是

**权限标识**: `system:post:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "postId": 1,
    "postCode": "ceo",
    "postName": "董事长",
    "postSort": 1,
    "status": "0",
    "remark": ""
  }
}
```

### 6.3 新增岗位

**接口地址**: `POST /system/post`

**是否认证**: 是

**权限标识**: `system:post:add`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| postCode | string | 是 | 岗位编码 |
| postName | string | 是 | 岗位名称 |
| postSort | int | 是 | 显示顺序 |
| status | string | 否 | 状态 |
| remark | string | 否 | 备注 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "新增成功"
}
```

### 6.4 修改岗位

**接口地址**: `PUT /system/post`

**是否认证**: 是

**权限标识**: `system:post:edit`

**请求参数**: 同新增，需包含postId

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 6.5 删除岗位

**接口地址**: `DELETE /system/post/{postIds}`

**是否认证**: 是

**权限标识**: `system:post:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

### 6.6 获取岗位选择列表

**接口地址**: `GET /system/post/optionselect`

**是否认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "postId": 1,
      "postCode": "ceo",
      "postName": "董事长"
    }
  ]
}
```

---

## 7. 字典管理

### 7.1 获取字典类型列表

**接口地址**: `GET /system/dict/type/list`

**是否认证**: 是

**权限标识**: `system:dict:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |
| dictName | string | 否 | 字典名称 |
| dictType | string | 否 | 字典类型 |
| status | string | 否 | 状态 |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 10,
    "rows": [
      {
        "dictId": 1,
        "dictName": "用户性别",
        "dictType": "sys_user_sex",
        "status": "0",
        "createTime": "2024-01-01 00:00:00"
      }
    ]
  }
}
```

### 7.2 获取字典类型详情

**接口地址**: `GET /system/dict/type/{dictId}`

**是否认证**: 是

**权限标识**: `system:dict:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "dictId": 1,
    "dictName": "用户性别",
    "dictType": "sys_user_sex",
    "status": "0",
    "remark": "用户性别字典"
  }
}
```

### 7.3 新增字典类型

**接口地址**: `POST /system/dict/type`

**是否认证**: 是

**权限标识**: `system:dict:add`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dictName | string | 是 | 字典名称 |
| dictType | string | 是 | 字典类型 |
| status | string | 否 | 状态 |
| remark | string | 否 | 备注 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "新增成功"
}
```

### 7.4 修改字典类型

**接口地址**: `PUT /system/dict/type`

**是否认证**: 是

**权限标识**: `system:dict:edit`

**请求参数**: 同新增，需包含dictId

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 7.5 删除字典类型

**接口地址**: `DELETE /system/dict/type/{dictIds}`

**是否认证**: 是

**权限标识**: `system:dict:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

### 7.6 刷新字典缓存

**接口地址**: `DELETE /system/dict/type/refreshCache`

**是否认证**: 是

**权限标识**: `system:dict:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "刷新成功"
}
```

### 7.7 获取字典选择列表

**接口地址**: `GET /system/dict/type/optionselect`

**是否认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "dictId": 1,
      "dictName": "用户性别",
      "dictType": "sys_user_sex"
    }
  ]
}
```

### 7.8 获取字典数据列表

**接口地址**: `GET /system/dict/data/list`

**是否认证**: 是

**权限标识**: `system:dict:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |
| dictType | string | 是 | 字典类型 |
| dictLabel | string | 否 | 字典标签 |
| status | string | 否 | 状态 |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 3,
    "rows": [
      {
        "dictCode": 1,
        "dictSort": 1,
        "dictLabel": "男",
        "dictValue": "0",
        "dictType": "sys_user_sex",
        "cssClass": "",
        "listClass": "primary",
        "isDefault": "Y",
        "status": "0",
        "createTime": "2024-01-01 00:00:00"
      }
    ]
  }
}
```

### 7.9 获取字典数据详情

**接口地址**: `GET /system/dict/data/{dictCode}`

**是否认证**: 是

**权限标识**: `system:dict:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "dictCode": 1,
    "dictSort": 1,
    "dictLabel": "男",
    "dictValue": "0",
    "dictType": "sys_user_sex",
    "cssClass": "",
    "listClass": "primary",
    "isDefault": "Y",
    "status": "0",
    "remark": "性别男"
  }
}
```

### 7.10 根据字典类型查询字典数据

**接口地址**: `GET /system/dict/data/type/{dictType}`

**是否认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "data": [
    {
      "dictCode": 1,
      "dictSort": 1,
      "dictLabel": "男",
      "dictValue": "0",
      "dictType": "sys_user_sex",
      "isDefault": "Y"
    }
  ]
}
```

### 7.11 新增字典数据

**接口地址**: `POST /system/dict/data`

**是否认证**: 是

**权限标识**: `system:dict:add`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dictType | string | 是 | 字典类型 |
| dictLabel | string | 是 | 字典标签 |
| dictValue | string | 是 | 字典键值 |
| dictSort | int | 否 | 字典排序 |
| cssClass | string | 否 | 样式属性 |
| listClass | string | 否 | 表格回显样式 |
| isDefault | string | 否 | 是否默认（Y是 N否） |
| status | string | 否 | 状态 |
| remark | string | 否 | 备注 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "新增成功"
}
```

### 7.12 修改字典数据

**接口地址**: `PUT /system/dict/data`

**是否认证**: 是

**权限标识**: `system:dict:edit`

**请求参数**: 同新增，需包含dictCode

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 7.13 删除字典数据

**接口地址**: `DELETE /system/dict/data/{dictCodes}`

**是否认证**: 是

**权限标识**: `system:dict:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

---

## 8. 参数配置

### 8.1 获取参数配置列表

**接口地址**: `GET /system/config/list`

**是否认证**: 是

**权限标识**: `system:config:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |
| configName | string | 否 | 参数名称 |
| configKey | string | 否 | 参数键名 |
| configType | string | 否 | 系统内置（Y是 N否） |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 6,
    "rows": [
      {
        "configId": 1,
        "configName": "主框架页-默认皮肤样式名称",
        "configKey": "sys.index.skinName",
        "configValue": "skin-blue",
        "configType": "Y",
        "createTime": "2024-01-01 00:00:00"
      }
    ]
  }
}
```

### 8.2 获取参数配置详情

**接口地址**: `GET /system/config/{configId}`

**是否认证**: 是

**权限标识**: `system:config:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "configId": 1,
    "configName": "主框架页-默认皮肤样式名称",
    "configKey": "sys.index.skinName",
    "configValue": "skin-blue",
    "configType": "Y",
    "remark": "蓝色 skin-blue"
  }
}
```

### 8.3 根据参数键名查询参数值

**接口地址**: `GET /system/config/configKey/{configKey}`

**是否认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "data": "skin-blue"
}
```

### 8.4 新增参数配置

**接口地址**: `POST /system/config`

**是否认证**: 是

**权限标识**: `system:config:add`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| configName | string | 是 | 参数名称 |
| configKey | string | 是 | 参数键名 |
| configValue | string | 是 | 参数键值 |
| configType | string | 否 | 系统内置（Y是 N否） |
| remark | string | 否 | 备注 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "新增成功"
}
```

### 8.5 修改参数配置

**接口地址**: `PUT /system/config`

**是否认证**: 是

**权限标识**: `system:config:edit`

**请求参数**: 同新增，需包含configId

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 8.6 删除参数配置

**接口地址**: `DELETE /system/config/{configIds}`

**是否认证**: 是

**权限标识**: `system:config:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

### 8.7 刷新参数缓存

**接口地址**: `DELETE /system/config/refreshCache`

**是否认证**: 是

**权限标识**: `system:config:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "刷新成功"
}
```

---

## 9. 通知公告

### 9.1 获取通知公告列表

**接口地址**: `GET /system/notice/list`

**是否认证**: 是

**权限标识**: `system:notice:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |
| noticeTitle | string | 否 | 公告标题 |
| noticeType | string | 否 | 公告类型（1通知 2公告） |
| status | string | 否 | 状态 |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 2,
    "rows": [
      {
        "noticeId": 1,
        "noticeTitle": "欢迎使用WorkFlow工作流系统",
        "noticeType": "2",
        "status": "0",
        "createTime": "2024-01-01 00:00:00"
      }
    ]
  }
}
```

### 9.2 获取通知公告详情

**接口地址**: `GET /system/notice/{noticeId}`

**是否认证**: 是

**权限标识**: `system:notice:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "noticeId": 1,
    "noticeTitle": "欢迎使用WorkFlow工作流系统",
    "noticeType": "2",
    "noticeContent": "<p>欢迎使用WorkFlow工作流系统</p>",
    "status": "0",
    "remark": "管理员"
  }
}
```

### 9.3 新增通知公告

**接口地址**: `POST /system/notice`

**是否认证**: 是

**权限标识**: `system:notice:add`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| noticeTitle | string | 是 | 公告标题 |
| noticeType | string | 是 | 公告类型（1通知 2公告） |
| noticeContent | string | 否 | 公告内容 |
| status | string | 否 | 状态 |
| remark | string | 否 | 备注 |

**响应示例**:
```json
{
  "code": 200,
  "msg": "新增成功"
}
```

### 9.4 修改通知公告

**接口地址**: `PUT /system/notice`

**是否认证**: 是

**权限标识**: `system:notice:edit`

**请求参数**: 同新增，需包含noticeId

**响应示例**:
```json
{
  "code": 200,
  "msg": "修改成功"
}
```

### 9.5 删除通知公告

**接口地址**: `DELETE /system/notice/{noticeIds}`

**是否认证**: 是

**权限标识**: `system:notice:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

---

## 10. 操作日志

### 10.1 获取操作日志列表

**接口地址**: `GET /monitor/operlog/list`

**是否认证**: 是

**权限标识**: `monitor:operlog:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |
| title | string | 否 | 模块标题 |
| operName | string | 否 | 操作人员 |
| businessType | int | 否 | 业务类型 |
| status | int | 否 | 操作状态 |
| beginTime | string | 否 | 开始时间 |
| endTime | string | 否 | 结束时间 |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 100,
    "rows": [
      {
        "operId": 1,
        "title": "用户管理",
        "businessType": 1,
        "method": "add()",
        "requestMethod": "POST",
        "operName": "admin",
        "deptName": "研发部门",
        "operUrl": "/system/user",
        "operIp": "127.0.0.1",
        "operLocation": "内网IP",
        "status": 0,
        "operTime": "2024-01-01 12:00:00",
        "costTime": 100
      }
    ]
  }
}
```

### 10.2 获取操作日志详情

**接口地址**: `GET /monitor/operlog/{operId}`

**是否认证**: 是

**权限标识**: `monitor:operlog:query`

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "operId": 1,
    "title": "用户管理",
    "businessType": 1,
    "method": "add()",
    "requestMethod": "POST",
    "operatorType": 0,
    "operName": "admin",
    "deptName": "研发部门",
    "operUrl": "/system/user",
    "operIp": "127.0.0.1",
    "operLocation": "内网IP",
    "operParam": "{}",
    "jsonResult": "{}",
    "status": 0,
    "errorMsg": null,
    "operTime": "2024-01-01 12:00:00",
    "costTime": 100
  }
}
```

### 10.3 删除操作日志

**接口地址**: `DELETE /monitor/operlog/{operIds}`

**是否认证**: 是

**权限标识**: `monitor:operlog:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

### 10.4 清空操作日志

**接口地址**: `DELETE /monitor/operlog/clean`

**是否认证**: 是

**权限标识**: `monitor:operlog:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "清空成功"
}
```

### 10.5 导出操作日志

**接口地址**: `POST /monitor/operlog/export`

**是否认证**: 是

**权限标识**: `monitor:operlog:export`

**请求参数**: 同列表查询参数

**响应**: Excel文件下载

---

## 11. 登录日志

### 11.1 获取登录日志列表

**接口地址**: `GET /monitor/logininfor/list`

**是否认证**: 是

**权限标识**: `monitor:logininfor:list`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |
| userName | string | 否 | 用户账号 |
| ipaddr | string | 否 | 登录IP |
| status | string | 否 | 登录状态 |
| beginTime | string | 否 | 开始时间 |
| endTime | string | 否 | 结束时间 |

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "total": 50,
    "rows": [
      {
        "infoId": 1,
        "userName": "admin",
        "ipaddr": "127.0.0.1",
        "loginLocation": "内网IP",
        "browser": "Chrome",
        "os": "Windows 10",
        "status": "0",
        "msg": "登录成功",
        "loginTime": "2024-01-01 12:00:00"
      }
    ]
  }
}
```

### 11.2 删除登录日志

**接口地址**: `DELETE /monitor/logininfor/{infoIds}`

**是否认证**: 是

**权限标识**: `monitor:logininfor:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "删除成功"
}
```

### 11.3 清空登录日志

**接口地址**: `DELETE /monitor/logininfor/clean`

**是否认证**: 是

**权限标识**: `monitor:logininfor:remove`

**响应示例**:
```json
{
  "code": 200,
  "msg": "清空成功"
}
```

### 11.4 导出登录日志

**接口地址**: `POST /monitor/logininfor/export`

**是否认证**: 是

**权限标识**: `monitor:logininfor:export`

**响应**: Excel文件下载

### 11.5 解锁用户

**接口地址**: `DELETE /monitor/logininfor/unlock/{userName}`

**是否认证**: 是

**权限标识**: `monitor:logininfor:unlock`

**响应示例**:
```json
{
  "code": 200,
  "msg": "解锁成功"
}
```