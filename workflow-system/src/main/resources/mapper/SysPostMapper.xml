<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clx.workflow.system.mapper.SysPostMapper">

    <resultMap type="SysPost" id="SysPostResult">
        <id     property="postId"     column="post_id"     />
        <result property="postCode"   column="post_code"   />
        <result property="postName"   column="post_name"   />
        <result property="postSort"   column="post_sort"   />
        <result property="status"     column="status"      />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
        <result property="remark"     column="remark"      />
    </resultMap>

    <select id="selectPostsByUserId" resultMap="SysPostResult">
        select p.post_id, p.post_code, p.post_name, p.post_sort, p.status, p.create_time, p.remark
        from sys_post p
        left join sys_user_post up on p.post_id = up.post_id
        left join sys_user u on up.user_id = u.user_id
        where u.user_id = #{userId}
        order by p.post_sort
    </select>

    <select id="checkPostCodeUnique" resultMap="SysPostResult">
        select p.post_id, p.post_code, p.post_name, p.post_sort, p.status, p.create_time, p.remark
        from sys_post p
        where p.post_code = #{postCode} limit 1
    </select>

</mapper>