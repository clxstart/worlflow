<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clx.workflow.system.mapper.SysRoleMapper">

    <resultMap type="com.clx.workflow.system.domain.SysRole" id="SysRoleResult">
        <id property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="status" column="status"/>
        <result property="deleted" column="deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectRoleVo">
        select role_id, role_name, role_key, role_sort, status, deleted, create_time, update_time, remark
        from sys_role
    </sql>

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectRolesByUserId" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where deleted = 0 and role_id in (
            select role_id from sys_user_role where user_id = #{userId}
        )
        order by role_sort
    </select>

    <!-- 根据用户ID查询角色权限字符串 -->
    <select id="selectRoleKeyByUserId" resultType="String">
        select r.role_key
        from sys_role r
        left join sys_user_role ur on r.role_id = ur.role_id
        where ur.user_id = #{userId} and r.deleted = 0 and r.status = '0'
    </select>

    <!-- 根据用户ID查询角色ID列表 -->
    <select id="selectRoleIdListByUserId" resultType="Long">
        select role_id from sys_user_role where user_id = #{userId}
    </select>

    <!-- 校验角色名称是否唯一 -->
    <select id="checkRoleNameUnique" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where role_name = #{roleName} and deleted = 0 limit 1
    </select>

    <!-- 校验角色权限是否唯一 -->
    <select id="checkRoleKeyUnique" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where role_key = #{roleKey} and deleted = 0 limit 1
    </select>

</mapper>